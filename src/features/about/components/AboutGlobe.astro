<div class="about-globe" data-about-globe>
  <div class="about-globe__fallback" aria-hidden="true">
    <span></span><span></span><i></i>
  </div>
  <p class="sr-only">
    Interactive global network visualization with Bogotá, Colombia, as its origin.
  </p>
</div>

<style>
  .about-globe {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: visible;
    cursor: grab;
    touch-action: pan-y;
  }

  .about-globe:active {
    cursor: grabbing;
  }

  .about-globe :global(canvas) {
    width: 100%;
    height: 100%;
  }

  .about-globe.is-ready .about-globe__fallback {
    opacity: 0;
  }

  .about-globe__fallback {
    position: absolute;
    inset: 8%;
    border: 1px solid rgb(242 242 242 / 18%);
    border-radius: 50%;
    transition: opacity 240ms ease;
  }

  .about-globe__fallback span {
    position: absolute;
    top: 50%;
    left: 50%;
    border: 1px solid rgb(242 242 242 / 10%);
    border-radius: 50%;
    transform: translate(-50%, -50%);
  }

  .about-globe__fallback span:first-child {
    width: 38%;
    height: 100%;
  }

  .about-globe__fallback span:nth-child(2) {
    width: 100%;
    height: 38%;
  }

  .about-globe__fallback i {
    position: absolute;
    top: 48%;
    left: 30%;
    width: 0.38rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--color-signal);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .about-globe__fallback {
      transition: none;
    }
  }
</style>
