<div class="globe-shell" data-globe data-hero-globe>
  <div class="globe-fallback" aria-hidden="true">
    <span class="fallback-axis fallback-axis--vertical"></span>
    <span class="fallback-axis fallback-axis--horizontal"></span>
    <span class="fallback-orbit fallback-orbit--one"></span>
    <span class="fallback-orbit fallback-orbit--two"></span>
    <span class="fallback-node"></span>
  </div>
  <p class="sr-only">
    Red internacional con origen en Bogotá, Colombia, y conexiones activas con distintos países.
  </p>
</div>

<style>
  .globe-shell {
    position: relative;
    width: 100%;
    height: 100%;
    overflow: visible;
    cursor: grab;
    touch-action: pan-y;
  }

  .globe-shell:active {
    cursor: grabbing;
  }

  .globe-shell :global(canvas) {
    width: 100%;
    height: 100%;
  }

  .globe-shell.is-ready .globe-fallback {
    opacity: 0;
  }

  .globe-fallback {
    position: absolute;
    top: 50%;
    left: 50%;
    width: min(84%, 42rem);
    aspect-ratio: 1;
    border: 1px solid rgb(242 242 242 / 34%);
    border-radius: 50%;
    opacity: 0.8;
    transform: translate(-50%, -50%);
    transition: opacity 240ms ease;
  }

  .fallback-axis,
  .fallback-orbit {
    position: absolute;
    inset: 50% auto auto 50%;
    border: 1px solid rgb(242 242 242 / 18%);
    border-radius: 50%;
    transform: translate(-50%, -50%);
  }

  .fallback-axis--vertical {
    width: 34%;
    height: 100%;
  }

  .fallback-axis--horizontal {
    width: 100%;
    height: 34%;
  }

  .fallback-orbit--one {
    width: 70%;
    height: 100%;
  }

  .fallback-orbit--two {
    width: 100%;
    height: 70%;
  }

  .fallback-node {
    position: absolute;
    top: 51%;
    left: 35%;
    width: 0.4rem;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--color-signal);
    box-shadow: 0 0 0 0.35rem rgb(225 6 0 / 12%);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .globe-fallback {
      transition: none;
    }
  }
</style>
